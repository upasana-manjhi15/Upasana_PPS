#include <stdio.h>
#define PI 3.14
int main()
{
  float a,b;
  printf("\n Enter value of a and b=");
  scanf("%f %f" ,&a,&b);
  printf("\n addition is: %f",a+b);
  printf("\n sum is: %f",a-b);
  printf("\n mul is: %f",a*b);
  printf("\n div is: %f",a/b);
  printf("\n module is: %d",(int)a%(int)b);
  return 0;
}
